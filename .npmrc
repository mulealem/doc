fund=false
audit=false
# fumadocs-core@15 declares `peerOptional next@"14.x.x || 15.x.x"`, but this
# project pins next@16.2.6 (works fine in practice). Without this flag npm
# hard-fails with ERESOLVE during fresh installs (e.g. the Docker build).
legacy-peer-deps=true
